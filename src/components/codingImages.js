import React from "react"

const CodingImageContainer = ({children}) => {

  return (
    <div>
      {children}
    </div>
  )
}

const ContainerStyle = styled.div`
  
`

export default CodingImageContainer
